# Changelog

## 1.0.0

- Initial release: DALI bridge admin UI in the Ctrlable Pro sidebar via ingress.
- Multiple bridges, with a picker showing live reachability, version and bus count.
- Bridge basic-auth credentials injected upstream — no browser password prompt.
- Defaults match a factory-fresh bridge (`admin` / `admin`).
